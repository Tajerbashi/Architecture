@{
    ViewData["Title"] = "Home Page";
    var Response = ViewBag.Response;
    var Content = ViewBag.Content;
}

<div class="container">
    <div class="row">
        <div class="d-flex flex-row justify-content-center align-items-center flex-wrap">
            @Response
        </div>
        <div class="d-flex flex-row justify-content-center align-items-center flex-wrap">
            @Content
            @*@foreach (var item in Content)
            {
                    <div class="px-5 py-2 m-2 rounded-3 bg-danger bg-gradient text-light">@item</div>
            }*@
        </div>
    </div>
</div>